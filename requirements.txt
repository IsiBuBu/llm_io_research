# Core library for interacting with the Gemini API
google-generativeai>=0.3.0

# Used for numerical operations, especially in data generation and metrics
numpy>=1.21.0

# Used for data manipulation, analysis, and creating dataframes
pandas>=1.3.0

# Required for statistical analysis, particularly the correlation tests
scipy>=1.7.0

# Required for generating plots and visualizations
matplotlib>=3.5.0
seaborn>=0.11.0